services:
  backend:
    build: ./backend
    environment:
      APP_ENV: development
      APP_PORT: 8080
      APP_DATABASE_PATH: /data/app.db
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-change-this-development-key}
      CORS_ALLOWED_ORIGINS: http://localhost:${FRONTEND_PORT:-13000}
      DEFAULT_QUERY_TIMEOUT_SECONDS: 30
      MAX_QUERY_ROWS: 200
      MAX_OPEN_CONNECTIONS: 10
      MAX_CONCURRENT_QUERIES: 4
    ports:
      - "${BACKEND_PORT:-18080}:8080"
    volumes:
      - app_data:/data
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 10s
      timeout: 3s
      retries: 5
  frontend:
    build: ./frontend
    environment:
      NUXT_PUBLIC_API_BASE: http://localhost:${BACKEND_PORT:-18080}/api
    ports:
      - "${FRONTEND_PORT:-13000}:3000"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3000').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 10s
      timeout: 3s
      retries: 5
volumes:
  app_data:
